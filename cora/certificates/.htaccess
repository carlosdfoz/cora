# Arquivo .htaccess para pasta /certificates/
# Sistema de Gestão de Boletos - Tray Sistemas
# 
# INSTRUÇÕES:
# 1. Crie este arquivo dentro da pasta /certificates/
# 2. Nome do arquivo: .htaccess (com o ponto no início)
# 3. Salve como texto simples (sem extensão)

# Bloquear acesso direto a todos os arquivos da pasta
Order Deny,Allow
Deny from all

# Bloquear especificamente arquivos de certificados
<FilesMatch "\.(pem|key|crt|p12|pfx)$">
    Order Deny,Allow
    Deny from all
</FilesMatch>

# Proteger contra listagem de diretório
Options -Indexes

# Desabilitar execução de scripts
Options -ExecCGI
AddHandler cgi-script .php .pl .py .jsp .asp .sh .cgi

# Cabeçalhos de segurança
<IfModule mod_headers.c>
    Header always set X-Content-Type-Options nosniff
    Header always set X-Frame-Options DENY
    Header always set X-XSS-Protection "1; mode=block"
</IfModule>

# Proteger contra acesso via HTTP methods
<Limit GET POST>
    Deny from all
</Limit>